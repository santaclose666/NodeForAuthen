import React, {useState, useLayoutEffect, memo, useRef} from 'react';
import {
    View,
    Text,
    Image,
    TouchableOpacity,
    StyleSheet,
    SafeAreaView,
    TextInput,
    ScrollView,
    FlatList,
  } from 'react-native';
  import {useSelector} from 'react-redux';